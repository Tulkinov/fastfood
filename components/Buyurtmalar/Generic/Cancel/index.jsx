import React from "react";
import { CancelIcon, Container } from "./styled";

export const Cancel = () => {
  return (
    <Container>
      <CancelIcon />
    </Container>
  );
};

export default Cancel;
